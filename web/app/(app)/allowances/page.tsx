import { PhaseStub } from "@/components/layout/phase-stub";

export default function AllowancesPage() {
  return (
    <PhaseStub
      title="Allowances"
      description="Track allowances separately from salary — recurring or one-off, each with its own date."
    />
  );
}
