import { PhaseStub } from "@/components/layout/phase-stub";

export default function SettingsPage() {
  return (
    <PhaseStub
      title="Settings"
      description="Your profile, password, two-factor login, and default currency."
      phase="Phase 2 (Foundation) / Phase 5"
    />
  );
}
