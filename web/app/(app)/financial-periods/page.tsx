import { PhaseStub } from "@/components/layout/phase-stub";

export default function FinancialPeriodsPage() {
  return (
    <PhaseStub
      title="Financial Periods"
      description="Every month you've tracked, with its status, and a way to open, close, or reopen it."
    />
  );
}
