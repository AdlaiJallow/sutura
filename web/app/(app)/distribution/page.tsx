import { PhaseStub } from "@/components/layout/phase-stub";

export default function DistributionPage() {
  return (
    <PhaseStub
      title="Distribution"
      description="Decide what share of your income goes to Needs, Wants, Savings, and your own categories — must always sum to 100%."
    />
  );
}
