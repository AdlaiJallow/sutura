import { PhaseStub } from "@/components/layout/phase-stub";

export default function ExpensesPage() {
  return (
    <PhaseStub
      title="Expenses"
      description="Every expense, linked to a distribution category, with receipts attached and overspending flagged clearly."
    />
  );
}
