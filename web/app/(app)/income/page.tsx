import { PhaseStub } from "@/components/layout/phase-stub";

export default function IncomePage() {
  return (
    <PhaseStub
      title="Other Income"
      description="Anything else that comes in — freelance work, gifts, refunds — kept separate from salary and allowances."
    />
  );
}
