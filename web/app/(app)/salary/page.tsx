import { PhaseStub } from "@/components/layout/phase-stub";

export default function SalaryPage() {
  return (
    <PhaseStub
      title="Salary"
      description="Record your net salary for this period — one entry per month, feeding straight into your income total."
    />
  );
}
